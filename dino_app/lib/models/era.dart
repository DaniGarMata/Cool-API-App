class Era {
  String name, description, imageURL;
  int start, end;

  Era({
    required this.name,
    required this.description,
    required this.imageURL,
    required this.start,
    required this.end
  });
}